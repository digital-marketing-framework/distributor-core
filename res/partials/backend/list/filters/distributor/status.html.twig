{% set labelMap = {
    doneNotSkipped: 'Done (processed)',
    doneSkipped: 'Done (skipped)',
    failed: 'Failed',
    queued: 'Queued',
    pending: 'Pending',
    running: 'Running'
} %}
{{ include('list/filters/checkboxes.html.twig', {name:'status',filters,filterBounds,labelMap:labelMap}) }}
