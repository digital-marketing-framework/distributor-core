{{ include('section/tabs.html.twig', {current, tabs: [
    {action: 'show-statistics', label: 'Statistics', url: uri('page.distributor.show-statistics')},
    {action: 'show-errors', label: 'Errors', url: uri('page.distributor.show-errors')},
    {action: 'list', label: 'List', url: uri('page.distributor.list')},
    {action: 'list-failed', label: 'List failed', icon: 'link', url: uri('page.distributor.list-failed')},
    {action: 'list-stuck', label: 'List stuck', icon: 'link', url: uri('page.distributor.list-stuck')},
    {action: 'list-expired', label: 'List expired', icon: 'link', url: uri('page.distributor.list-expired')}
]}) }}
